apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: lan-only
  namespace: openclaw
spec:
  ipAllowList:
    sourceRange:
      - 192.168.2.0/24
      # - 100.64.0.0/10  # (optional) tailscale CGNAT range if you ever want tailscale too