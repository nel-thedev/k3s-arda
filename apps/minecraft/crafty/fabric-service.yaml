apiVersion: v1
kind: Service
metadata:
  name: fabric
  namespace: minecraft
spec:
  type: ClusterIP
  selector:
    app: crafty
  ports:
    - name: minecraft
      port: 25565
      targetPort: 25565
      protocol: TCP
